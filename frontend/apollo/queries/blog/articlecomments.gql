query ArticleComments($slug: String!) {
  articles(where: { slug: $slug }) {
    slug
    id
    comments {
      content
      points
      authorId
      authorUser {
        id
        email
        username
        provider
      }
      authorType
      authorName
      authorEmail
      authorAvatar
    }
  }
}
